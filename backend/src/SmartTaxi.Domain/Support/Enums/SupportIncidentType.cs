namespace SmartTaxi.Domain.Support.Enums;

/// <summary>Exact types from the business specification's Module 10 (Incidents) section — none invented.</summary>
public enum SupportIncidentType
{
    SafetyIncident,
    VehicleIncident,
    RideIncident,
    PaymentIncident,
    TechnicalIncident,
    SecurityIncident,
    FraudIncident,
    OperationalIncident
}
